__pycache__/
*.py[cod]
.venv/
venv/
.DS_Store
*.egg-info/

# Large frozen competition artifacts: download them from the participant kit
# release rather than committing them. See README.md > Setup.
data/catalog.jsonl
data/catalog.jsonl.gz

# Superseded prototype, kept locally for history but not part of the
# submission. See README.md > Project layout.
legacy/

# Local evaluation output. Committed results live in artifacts/ and are
# regenerated by `make verify`; scratch runs are not.
artifacts/scratch/
